services:
  api:
    build: .
    env_file: .env
    ports:
      - "5001:5000"  # map host 5001 to container 5000 if port 5000 is busy
    depends_on:
      - mongo

  mongo:
    image: mongo:6.0
    restart: always
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: